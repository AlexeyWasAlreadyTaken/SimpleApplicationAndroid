<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/textPlateImageView"
        android:layout_width="111dp"
        android:layout_height="43dp"
        android:layout_marginStart="5dp"
        android:contentDescription="@string/PLACEHOLDER"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0"
        app:srcCompat="@drawable/fltrtextbg" />

    <TextView
        android:id="@+id/categoryTitle"
        android:layout_width="46dp"
        android:layout_height="22dp"
        android:fontFamily="Casual"
        android:gravity="center"
        android:text="@string/PLACEHOLDER"
        android:textColor="@color/black"
        android:textSize="14dp"
        app:layout_constraintBottom_toBottomOf="@+id/textPlateImageView"
        app:layout_constraintEnd_toEndOf="@+id/textPlateImageView"
        app:layout_constraintStart_toStartOf="@+id/textPlateImageView"
        app:layout_constraintTop_toTopOf="@+id/textPlateImageView" />

</androidx.constraintlayout.widget.ConstraintLayout>